# Alpha Yard Website

Static marketing website for Alpha Yard, built with plain HTML, CSS, and vanilla JavaScript.

## Preview locally

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080/`.

## Files

- `index.html` - responsive landing page and primary website entry point
- `OnePager` - investor one-pager referenced from the website navigation
